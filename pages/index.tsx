import type { GetStaticProps, NextPage } from 'next'
import Head from 'next/head'
import Searchbox from '../src/Searchbox'
import { trending } from '../src/api'
import Image from "next/image"
import Card from '../src/Card'

interface Props {
  gifs: any;
}

const Home: NextPage<Props> = ({ gifs }) => {

  return (
    <div className="container text-center justify-center">
      <Head>
        <title>Create Next App</title>
        <meta name="description" content="Generated by create next app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <div className='justify-center align-center bg-blue-200'>
        <h1 className='text-2xl font-bold underline'>ADVANCED JS STUDY GROUP APP</h1>
        <Searchbox />
        <div className='grid grid-cols-3'>
          {gifs.map((item:any)=> <Card key={item.id} gif={item} />)}
        </div>
      </div>
    </div>
  )
}
//.bitly_gif_url
//.bitly_url
//[0].embed_url
//[0].url
export const getStaticProps: GetStaticProps = async () => {
  const gifs = await trending();

  return {
    props: {
      gifs,
    },
  };
};

export default Home
